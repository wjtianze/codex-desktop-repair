
import{Ubt as __localSavedProjects}from"./app-initial-6c4523b43a11.js";import{__localCreateSavedCompactThread,__localResumeSavedCompactThread}from'./app-initial-6c4523b43a11.js';
function __localSavedTarget(scope,options){const projects=scope.get(__localSavedProjects),normalize=value=>typeof value==='string'?value.replaceAll('\\','/').replace(/\/$/,''):null;const project=options.projectId!=null?projects.find(p=>p.projectId===options.projectId):options.cwd!=null?projects.find(p=>((p.hostId??'local')==='local'?normalize(p.path)?.toLowerCase()===normalize(options.cwd)?.toLowerCase():normalize(p.path)===normalize(options.cwd))&&(p.hostId??'local')===(options.hostId??'local')):null;if(options.projectId!=null&&!project)throw Error('The selected project is no longer available');return project==null?{type:'projectless'}:project.projectKind==='remote'?{type:'remoteProject',projectId:project.projectId,hostId:project.hostId,path:project.path}:{type:'project',projectId:project.projectId}}
async function __localSavedCompactCreation(scope,options={}){const outcome=await __localCreateSavedCompactThread(scope,__localSavedTarget(scope,options)),result=outcome.result;if(result.status!=='created')throw Error('Conversation creation was not confirmed');return{conversationId:result.conversationId,hostId:options.hostId??'local',creation:result}}
async function Y(scope,Component,options={}){if(options.prepareConversation||options.__localTabUpdate)return __localTemporarySide(scope,Component,options);Qt();const panel=pe(options.target??'right'),requested=options.existingConversation,requestedKey=requested?(requested.kind==='chat'?'saved-chat:':'sidechat:')+requested.id:null,opened=requestedKey==null?null:scope.get(panel.tabById$,requestedKey);if(opened){panel.openTab(scope,$,{...opened,id:requestedKey,props:opened.props,target:panel.panelId,revealAndFocus:options.activate!==false});return requested.id}const key='saved-side:'+crypto.randomUUID(),pending=panel.openPendingTab(scope,Zt,{id:key,title:options.displayTitle??(/^zh/i.test(options.intl?.locale??'')?'新对话':'New chat'),revealAndFocus:options.activate!==false,target:panel.panelId,props:{debugName:'SavedSideChat.pending',fillParent:true}});try{const existing=options.existingConversation;let created;if(existing?.kind==='local'){await __localResumeSavedCompactThread(scope,existing);created={conversationId:existing.id}}else if(existing?.kind==='chat'){created={conversationId:existing.id}}else created=await __localSavedCompactCreation(scope,options);if(!pending.isCurrent())return created.conversationId;const cloud=existing?.kind==='chat',props={SideChatTab:Component,conversationId:cloud?null:created.conversationId,__localCloudId:cloud?created.conversationId:undefined,__localCloudOrigin:cloud?existing.origin:undefined,target:panel.panelId,__localPersistent:true};const didComplete=pending.complete($,{id:(cloud?'saved-chat:':'sidechat:')+created.conversationId,title:options.displayTitle??(/^zh/i.test(options.intl?.locale??'')?'新对话':'New chat'),target:panel.panelId,props,onMove:(_scope,next)=>({props:{...props,target:next.panelId}})});if(!didComplete&&pending.isCurrent())pending.fail(options.onCreationError);return created.conversationId}catch(error){pending.fail(options.onCreationError);throw error}}
export{__localSavedCompactCreation};
