
export async function __localCreateSavedCompactThread(scope,target){tki();return VOi({scope,startWithoutFirstTurn:{},prompt:'',target,threadSource:'user',turnTrigger:'app_tool_create_thread'})}
export async function __localResumeSavedCompactThread(scope,{id,hostId='local',cwd}){tki();const manager=scope.get(UE,hostId);if(!manager||typeof manager.then==='function'||typeof manager.getConversation!=='function')throw Error('The local conversation manager is unavailable');const state=manager.getConversation(id);if(state?.ephemeral===true)throw Error('This conversation is temporary');if(state?.resumeState!=='resumed'){const directory=state?.cwd??cwd??(await manager.sendRequest('thread/read',{threadId:id,includeTurns:false})).thread.cwd;if(typeof directory!=='string'||!directory)throw Error('The saved conversation directory is unavailable');const result=await manager.resumeConversationForUnavailableOwner({conversationId:id,model:null,reasoningEffort:null,serviceTier:null,workspaceRoots:[directory],collaborationMode:state?.latestCollaborationMode??null});if(result?.status==='not-ready')throw Error('The saved conversation is not ready')}return{id,hostId}}
