# 兼容性与验证

## 验证组合

| 项目 | 版本 |
| --- | --- |
| 系统 | Windows x64 |
| 微软商店包 | OpenAI.Codex 26.908.4834.0 |
| ChatGPT App 界面 | 26.908.40834，Owl 运行外壳 |
| App 内置 Codex 后端 | 0.154.0-alpha.6.2 |
| 独立 Codex CLI | 0.154.0 |

App 和独立 CLI 使用不同的可执行文件。安装修复包不会替换独立 CLI，也不会用旧后端覆盖 App 自带后端。安装前仍校验官方签名、版本与精确文件摘要；其他 App 版本会停止安装。

## 检查范围

自动检查覆盖补丁边界、完整与无侧栏构建、原生脚本语法、草稿保存、请求调度、聊天位置、模型选择、消息与图片、文件引用、搜索过程、侧聊编辑和导航布局。

安装与恢复另检查跨版本运行副本、任务栏入口、原资料目录保留、备份完整性，以及恢复被中断后能否继续。新版浏览器已经移除旧记录文件监听器，相关旧补丁不再应用；未改动的官方浏览器程序文件仍验证摘要。

从两套实际可执行文件分别生成协议结构，并验证初始化、账号读取、模型列表和权限需求读取。协议语义参考 [OpenAI App Server 文档](https://learn.chatgpt.com/docs/app-server)；版本专用接口以实际生成结构为准。本次兼容性检查未启动真实模型回合。

隔离副本的模块加载和页面启动检查，与用户资料目录下的实际安装验收分别记录。自动检查不能替代所有真实对话、设备和长时间运行场景；仍有的限制见 [已知问题](AUDIT.md)。

## 本地检查

普通使用者可运行 **检查环境.cmd**。维护者使用 Node.js 24：

~~~powershell
node tests/run.cjs
node tests/run.cjs --installed
~~~

第二条命令要求安装受支持的 Windows 官方客户端，会重新生成原生夹具并执行实际修补函数和浏览器布局测试。测试结果与构建产物只保存在忽略的 build/ 目录中，不上传账户文件、聊天数据或完整官方资源。

## 本次发行验证

中英文两版各通过 447 项基于本机官方源代码的检查，完整与无侧栏构建分别校验 9,030 与 9,029 个打包资源。中文版 1.1.6 已沿用原资料目录安装，实际页面与全部修补模块加载通过，没有出现登录页或错误边界。英文版独立构建和测试，未覆盖本机中文版安装。

另使用真实客户端连续验证了编辑和重新生成：标签编号、标题与数量保持不变；项目选择确实应用维护目录，主导航保持不变。测试只使用新建临时线程，并拦截最终原生模型发送，没有消耗真实模型回合。

1.1.6 新增快速聊天内层边距检查，以及“最新”五个起始档位与五个预览档位的全部 25 种组合、具体模型标签和取消恢复检查。实际快速聊天窗口测得正文左边距 32 个界面像素、刻度与正文间距 4 像素，无横向溢出。

快速聊天搜索崩溃已用出错窗口保留的数据快照直接复现。同一快照在安装后的 1.1.6 中，普通／侧栏渲染模式及生成中／完成状态四种组合均转换成功。新增原生回归检查确认旧绑定会抛错，修正后保留可用的搜索来源；这些检查未启动新的模型回合。

## 1.1.7 ????

458 ??????????????????????????????????????????????????????????????? 9,030 ? 9,029 ???????????????????????????????

??? 1.1.7 ???????????????????????????????????????????????????? 330 ?? 350?370?390 ?????????????????????????????????????????????????????????????????
