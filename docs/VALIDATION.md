# 兼容性与验证

## 1.1.18 验证范围

232 项独立检查通过。图片引用专项使用当前客户端真实 Markdown 解析器验证 14 组合成输入，并对完整／无侧栏原生函数验证本地补全及服务端范围替换；未修改的官方函数能够复现孤立感叹号。现有 9 项消息图片检查、7 项文件引用组件检查和完整修补模块语法检查通过。

完整／无侧栏构建分别校验 15,316／15,315 个归档条目，官方版本、签名及安装预检通过。此次没有运行全部 `--installed` 套件。构建和解析器检查不等同于安装后的历史对话验收；安装状态另行记录。

2026-09-23 已完成本机事务安装，归档摘要匹配、原资料目录保留。重启后实际恢复到用户反馈的云端 Work 历史对话，截图及页面检查确认正文的孤立感叹号消失、PNG 附件卡片保留、输入框正常，无错误边界；验证没有发送或重新生成消息。其他三张反馈截图的历史页面未逐一打开，SVG／JPG 的路径由专项检查覆盖。

## 1.1.17 验证范围

最终源码新增两项专项检查，覆盖 Chat／云端页面作用域及搜索来源初始化；中英文两版各通过 218 项独立检查。此前版本适配通过 512 项整套回归，随后侧聊修正通过 6 项原生专项检查。为避免加重换页压力，另一次整套复跑已中止，不计为通过。

隔离客户端的真实首页及补丁模块能够加载；本地、Chat 和云端 Work 历史按原编号打开，历史搜索与无项目选项存在，未发送模型回合。重复历史标签修正另有专项回归。2026-09-23 已在日常资料目录完成 1.1.17 事务安装，运行摘要匹配且资料目录不变。实际侧聊的输入框、模式控件、无项目选项均已挂载；按新版可编辑节点测量，控件与编辑区域相距 50 CSS 像素，符合小于 60 像素的检查标准。安装后首次启动正常显示，验证未发送消息并恢复首页。此前隔离副本曾出现一次空白、重载后恢复，仍保留对其他冷启动场景的限制。

初次首页和空白会话检查未覆盖完整 Chat／云端 Work 渲染。后续真实故障定位到 queryClient 作用域为空及搜索来源初始化函数过期；2026-09-23 修正后重新安装，用户已确认普通 Chat、联网搜索 Chat 和云端 Work 恢复正常。此确认与自动检查分别记录，不代表全天性能或搜索时效已验证。

本次整机卡顿排查发现后台搜索持续运行而调用代码丢弃会话追踪信息。停止搜索并释放其它已结束任务后，用户确认明显恢复。此结果支持资源竞争的解释，不是已经证明或修复全部桌面内存泄漏。源码发行不包含本机诊断日志、私人聊天或全局任务规则。


## 1.1.16 验证

中英文两版各通过 511 项检查。新增覆盖正式会话创建、可选的无项目入口、项目及主机边界、关闭不丢弃、模式隔离和输入框几何。真实空白项目侧聊的后端标记为非临时，会话历史文件存在，关闭标签后仍可读取。实际客户端已加载本地输入框及两种云端模式，未发送模型回合，不将界面检查当作完整在线回复验收。


## 1.1.12 验证

中英文各 481 项检查通过。新增检查覆盖官方虚拟列表的可见范围、实际 React 挂载时序、屏外块几何与清理，以及 `api_tool/context_stuff` 的教材页分类。

对真实历史的离线重放，错误生成图片项从 49 项降为 0，回答文本逐字一致；安装后的对应页面显示正文与引用，实际挂载普通回答重试菜单组件，没有图片重试组件或错误提示。验证未发起新的模型回合。

一个带公式的长 Chat 页面从约 18,100 个节点、3 个挂载回合降到约 9,073 个节点、1 个挂载回合。独立菜单采样中，加号打开约 76–86 ms，模型菜单首次约 125 ms；这些是特定设备和页面状态下的数据，不是所有对话的延迟承诺。缓存状态、同时运行的检查和诊断全文读取会影响结果，不把不同条件下的数据换算成整体提速倍数。

已核对官方 [#46249](https://github.com/openai/codex/issues/46249) 的新版变慢反馈，以及 [#41166](https://github.com/openai/codex/issues/41166) 的 Windows 长线程布局问题。本地修补依据自身复现与采样，不宣称这些上游问题均已解决。

## 验证组合

| 项目 | 版本 |
| --- | --- |
| 系统 | Windows x64 |
| 微软商店包 | OpenAI.Codex 26.915.4065.0 |
| ChatGPT App 界面 | 26.915.31945，Owl 运行外壳 |
| App 内置 Codex 后端 | 0.155.0-alpha.9.2 |
| 独立 Codex CLI | 0.155.1 |

App 和独立 CLI 使用不同的可执行文件。安装修复包不会替换独立 CLI，也不会用旧后端覆盖 App 自带后端。安装前仍校验官方签名、版本与精确文件摘要；其他 App 版本会停止安装。

## 检查范围

自动检查覆盖补丁边界、完整与无侧栏构建、原生脚本语法、草稿保存、请求调度、聊天位置、模型选择、消息与图片、文件引用、搜索过程、侧聊编辑和导航布局。

安装与恢复另检查跨版本运行副本、任务栏入口、原资料目录保留、备份完整性，以及恢复被中断后能否继续。新版浏览器已经移除旧记录文件监听器，相关旧补丁不再应用；未改动的官方浏览器程序文件仍验证摘要。

分别使用两套实际可执行文件验证初始化、账号读取、模型列表和权限需求读取。协议语义参考 [OpenAI App Server 文档](https://learn.chatgpt.com/docs/app-server)，版本专用行为以对应可执行文件为准。本次兼容性检查未启动真实模型回合。

隔离副本的模块加载和页面启动检查，与用户资料目录下的实际安装验收分别记录。自动检查不能替代所有真实对话、设备和长时间运行场景；仍有的限制见 [已知问题](AUDIT.md)。

## 本地检查

普通使用者可运行 **检查环境.cmd**。维护者使用 Node.js 24：

~~~powershell
node tests/run.cjs
node tests/run.cjs --installed
~~~

第二条命令要求安装受支持的 Windows 官方客户端，会重新生成原生夹具并执行实际修补函数和浏览器布局测试。测试结果与构建产物只保存在忽略的 build/ 目录中，不上传账户文件、聊天数据或完整官方资源。

## 1.1.9 热修复验证

中英文两版各通过 469 项检查。新增回归直接使用官方 React／JSX 运行库，调用消息导航入口、阅读位置回调和本地对话操作入口，覆盖 1.1.8 遗漏的函数绑定问题。

隔离客户端实际打开本地 Codex 历史与普通 Chat 历史，两类页面均显示已有消息和输入框，没有错误边界或运行异常；没有发送或重发消息。中文版 1.1.9 已安装，原资料目录保留，实际页面和模块加载通过。

任务栏关联同时检查快捷方式及真实窗口属性，并验证 Owl 缺少旧接口时的原生回退路径。完整／无侧栏分别校验 14,928／14,927 个资源。仅首页正常或模块导入成功不足以证明对话路径正常。

## 1.1.8 发行验证

中文版通过 461 项基于本机官方资源的检查，包含完整与无侧栏补丁、原生函数、独立辅助模块、事务安装与浏览器布局。安装及启动新增内置后端摘要校验，避免继承旧的 CODEX_CLI_PATH。

App 内置后端与独立 CLI 分别通过初始化、账号读取、模型列表和权限要求读取，未启动模型回合。隔离客户端首页及全部修补模块加载通过；对话、侧聊、搜索、代码块和演示的原生回归单独记录，不能把模块导入等同于全部真实在线验收。

中文版已沿用原资料目录完成事务安装，实际页面与全部修补模块加载通过，无错误页或登录页。英文版独立构建与验证，不覆盖本机中文版安装。完整／无侧栏版本分别校验 14,926／14,925 个打包资源。
